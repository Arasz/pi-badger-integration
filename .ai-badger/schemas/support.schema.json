{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Arasz/ai-badger/schemas/support.schema.json",
  "title": "ai-badger agent capability matrix",
  "description": "What each supported agent can do and how much of it ai-badger scaffolds. Lives at features/common/support.json. Keep agent keys in sync with agents.schema.json and badger_lib.py AGENT_NAMES.",
  "type": "object",
  "required": [
    "description",
    "agents"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "lastUpdated": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO date the matrix was last reviewed against the agents' actual behaviour."
    },
    "agents": {
      "type": "object",
      "description": "Keyed by agent name — the same keys config.agents accepts.",
      "minProperties": 1,
      "propertyNames": {
        "enum": [
          "claude",
          "copilot",
          "hermes",
          "pi"
        ]
      },
      "additionalProperties": {
        "$ref": "#/$defs/agent"
      }
    }
  },
  "$defs": {
    "agent": {
      "type": "object",
      "required": [
        "name",
        "vendor",
        "capabilities"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name of the agent product."
        },
        "vendor": {
          "type": "string"
        },
        "capabilities": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/$defs/capability"
          }
        }
      }
    },
    "capability": {
      "type": "object",
      "required": [
        "supported",
        "mechanism",
        "aiBadgerSupport"
      ],
      "additionalProperties": false,
      "properties": {
        "supported": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "partial"
            }
          ],
          "description": "Whether the agent itself has this capability: true, false, or \"partial\"."
        },
        "mechanism": {
          "type": [
            "string",
            "null"
          ],
          "description": "How the agent exposes it; null when unsupported."
        },
        "aiBadgerSupport": {
          "type": "boolean",
          "description": "Whether ai-badger scaffolds anything for it. Never true when supported is false."
        },
        "configPath": {
          "type": "string",
          "description": "Project-relative file the capability is configured in."
        },
        "scaffoldedFile": {
          "type": "string",
          "description": "File ai-badger writes for this capability."
        },
        "scaffoldedBy": {
          "type": "string",
          "description": "Script and function that scaffolds it."
        },
        "wiredBy": {
          "type": "string",
          "description": "Script and function that registers it with the agent."
        },
        "format": {
          "type": [
            "string",
            "object"
          ],
          "description": "On-disk format when it is not implied by the mechanism — prose, or a shape sketch."
        }
      },
      "if": {
        "properties": {
          "supported": {
            "const": false
          }
        },
        "required": [
          "supported"
        ]
      },
      "then": {
        "properties": {
          "aiBadgerSupport": {
            "const": false
          }
        }
      }
    }
  }
}
