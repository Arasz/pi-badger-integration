{
  "name": "ai-badger-router-fallback",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Router-failure free-model fallback: on billing-exhaustion/auth/model-unavailable failures the session model advances once per episode over the Groq → Gemini → OpenRouter :free chain, session-only, never persisted. pi discovers the entry as ~/.pi/agent/extensions/<name>/index.ts; this manifest records metadata only and does not register the entry.",
  "main": "index.ts",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
