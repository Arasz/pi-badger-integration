{
  "name": "session-signals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Session signals for pi: marker ! importance (mid-run abort) + delegation working status in the footer. pi discovers the entry as ~/.pi/agent/extensions/<name>/index.ts; this manifest records metadata only and does not register the entry.",
  "main": "index.ts",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
