{
  "name": "ai-badger-update-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Integration release check: after session start, compare the installed marker against the latest pi-badger-integration GitHub release and post at most one notice per session when newer. Notify-only, never auto-installs; pi discovers the entry as ~/.pi/agent/extensions/<name>/index.ts; this manifest records metadata only and does not register the entry.",
  "main": "index.ts",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
