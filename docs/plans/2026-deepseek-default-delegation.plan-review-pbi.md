## Verdict

**READY-WITH-FOLDS** — the plan is executable and its gates are mostly falsifiable; four folds (all SHOULD) tighten sequencing, tripwire strength, recovery coverage, and publish hygiene. No blocker found.

---

**SHOULD — Precondition gate never verifies the framework release/tag, and the two annexes disagree about which tree `--root` points at**

Evidence: `plan-pbi.md:93-99` gates only on `$AI_BADGER/VERSION == 0.172.0` and canonical medium/high ids (main-checkout spelling `AI_BADGER=~/RiderProjects/ai-badger`); `plan-verification.md:105` says "Prefer the framework worktree's own refresh script with an explicit root" but `<fw>` is never defined; `plan.md` sequencing and R1 (`plan.md`, "Wave 2 … after the framework tag exists"; risk table R1) treat the tag as the gate. Failure scenario: PKG-I runs with `--root` = the PKG-F worktree before/without merge (VERSION and canonical content already look right), the framework PR then receives a review fold that changes the canonical (e.g. tail order), pbi commits a registry stamped `0.172.0` whose content does not match the released tag. Nothing catches it: no pbi test reads the repo registry (`plan-verification.md:101`), B4(i)/A5 use the same tree they refreshed from, and B4(iii) checks only ids/`frameworkVersion`, not canonical content equality. Concrete fix: run the gate on the merged tree after `git -C "$AI_BADGER" fetch --tags && git -C "$AI_BADGER" pull --ff-only`, plus `git -C "$AI_BADGER" describe --tags --exact-match` == `ai-badger--v0.172.0`; define `<fw>` once as that tree, or explicitly assert the worktree HEAD equals the tagged commit.

**SHOULD — The ADR-prose tripwire goes blind once medium and high collapse to the same id**

Evidence: `tests/subagent-level-integration.test.ts:74-76` loops `expect(text).toContain(pin)` over `[LOW_PREF, MED_PREF, HIGH_PREF]`; after the change `MED_PREF === HIGH_PREF === openrouter/deepseek/deepseek-v4.1-flash`. Failure scenario: update only the medium bullet in `docs/work/2026-09-06-pkg5-level-registry-adr.md:42`, leave `- high → …muse-spark-1.3-contributor` at `:43` — the text still contains glm (low) and deepseek (medium), so join 7a passes. `plan-pbi.md:147` (R3) claims join 7a "fails if the ADR lags the constants", and F3b (`plan-verification.md:129`) reverts *both* bullets, so the partial edit is never exercised. Concrete fix: assert the bullet lines or the absence of the retired id, e.g. `expect(text).toMatch(/- (medium|high) → `openrouter\/deepseek\/deepseek-v4\.1-flash`/g)` for both, and/or `expect(text).not.toContain("muse-spark-1.3-contributor")` (the proposed refresh note does not name it).

**SHOULD — Refresh review/revert path omits `.claude/**`, `.github/**`, and `HERMES.md`, so a catastrophic revert leaves a hybrid tree**

Evidence: worktree manifest contains 38 `.claude` targets, 40 `.github` targets, and 1 `HERMES.md` target (`python3 -c` over `.ai-badger/manifest.json`); `plan-pbi.md:111` lists only `AGENTS.override.md, CLAUDE.md, .hermes.md, .pi/agents/**`; `plan-pbi.md:119` review diff omits `.claude`/`.github`; `plan-pbi.md:140` revert is `git restore .ai-badger/ AGENTS.override.md CLAUDE.md .hermes.md .pi/`. New default-scope skills (H1) land as `.claude/skills/*` and `.github/skills/*` symlinks/dirs, and hook wiring re-renders `.github/hooks/ai-badger-hooks.json`, so this is expected churn, not hypothetical. (`.hermes.md` is tracked but not a manifest target; `HERMES.md` is.) Concrete fix: make `git revert <refresh-commit>` the primary recovery path and, for uncommitted churn, restore every managed root — `.ai-badger/ .claude/ .github/ .pi/ AGENTS.override.md CLAUDE.md HERMES.md .hermes.md` — plus `git clean -fd` on those paths for newly added files; add `.claude`/`.github` to the explicit review diff.

**SHOULD — `bun publish.ts` is specified from the main checkout while that checkout is dirty with unrelated work**

Evidence: `/Users/arasz/RiderProjects/pi-badger-integration` `git status --porcelain` shows ` M extensions/session-signals/index.ts` and `?? extensions/session-signals/silence-vertex-debug.ts` (+ test); `plan-verification.md:112` says "From the merged main checkout: `bun publish.ts`"; `publish.ts:45` publishes every extension dir including `session-signals`, and `--check` compares canonical==user scope, so the unrelated file installs and the check stays green. Also, B4(iii) notes the main checkout must be pulled, but B7 (`plan-verification.md:112`) does not. Concrete fix: `git pull --ff-only` and publish from a clean checkout/worktree at the merge commit (stash the session-signals work), then record `git rev-parse HEAD` in the probe record; the installed-copy B3 re-run remains the oracle.

**SHOULD — The rewritten frozen docstring keeps an unsubstantiated telemetry claim**

Evidence: the plan's proposed docstring retains "`{ frozen: true }` marks degraded resolutions in telemetry" (`plan-pbi.md:27`, final shape `:30-32`); in the source the flag is written at `delegation-core.ts:1075/1077` and read nowhere — `grep -rn '\.frozen' extensions/ tests/` returns no consumer, and `delegation-status.ts:33-34` states the status derivation owns the distinction "instead of widening the frozen registry record with a flag". The actual discriminator is `ModelGroupsLoad.source === "frozen"` (`delegation-core.ts:1226`). Concrete fix: drop the clause or replace it with "the degraded load is marked by `source: "frozen"` plus its warning; the type flag is inert" — PKG-P AC1 is the one place docstring truth is promised.

**INFO — The edit-time guard is tool-scoped; only the push-time gate backstops Bash writes**

Evidence: `generated_file_guard.py` guards `EDIT_TOOLS = ("Edit","Write","MultiEdit","NotebookEdit")` and is wired for pi via `.ai-badger/hooks/hooks.json` (PreToolUse matcher `Edit|Write|MultiEdit|NotebookEdit`) through `~/.pi/agent/extensions/ai-badger/hook-bridge.ts` deny handling; a Bash redirect (or `python3 -c` write) is not denied, though `gates/scaffold_freshness_guard.py` reds at push and the manifest hash will not match. No plan change required (the plan already forbids hand-patching); note it so a "quick fix via bash" isn't mistaken for a guarded path.

**INFO — B3's worktree variant does not resolve "its own node_modules"**

Evidence: `plan-verification.md:79` says importing the extension "resolves its own `node_modules`"; the worktree has none at root or `extensions/subagent/` (both absent), while the worktree sits under the main checkout, whose `node_modules/@earendil-works/*` is the ancestor Bun actually resolves (`bun build <worktree>/extensions/subagent/index.ts --target=bun` succeeds, 1826 modules). The installed-copy variant is self-contained. No action needed at the pinned path; if the worktree is ever relocated, run `bun install` + per-extension install (the CI dance) or use the installed copy first.

---

## Checked-clean

- **Frozen semantics:** low unchanged; medium/high re-pin to the id the 0.172.0 canonical will carry; `registryVersion: 1` stays truthful (canonical 1; 0.168.0 precedent, `plan-framework.md` step 1); "preferred pins only" matches the absence-rule contract; `openrouter/deepseek/deepseek-v4.1-flash` passes `MODEL_ID_PATTERN` (`delegation-core.ts:1049`, `:1081-1082`).
- **Test-impact completeness:** repo-wide `muse-spark` sweep (excluding plan docs) accounts for every occurrence — three named test files, the ADR, the frozen source, the manifest-managed registry. All other matches are FIXTURE data or `loaded.source` assertions (`tests/subagent-model-level.test.ts:105-107,147,151,174-176,228,307-310,351+`; `tests/subagent-level-integration.test.ts:60-61,88-89,93,181-182`); no other file pins medium/high for frozen; `tests/subagent-tool.test.ts` / `tests/subagent/subagent.test.ts` personas carry no level. No test reads the repo registry (`grep` + reading), so the plan's "suite gap → probe is the gate" conclusion is correct.
- **Frozen-path evidence claims:** queue harness writes only `<tmp>/.pi/agents` (queue test `:49-70`), resolves via `delegation-queue.ts:330` wired at `index.ts:1003` with `registry: loaded.registry` (`delegation-queue.ts:344`); delegate defaults never reached at runtime (`index.ts:1165,1167` pass the loaded registry). R2 ("frozen inert in-repo until refresh") holds.
- **Refresh mechanics:** command matches `den-refresh/SKILL.md:48`; `--root` wins the locator order (`refresh.py:31-33,112-120`); re-scaffold copies canonical every run with `seedOnce:false` (`model_registry.py`), stamps `config.frameworkVersion` (`scaffold.py:741`), and reports `reScaffolded` derived from the actual run; `.ai-badger.bckp/` is gitignored; `0.167.1 → 0.172.0` crosses no `BREAKING_VERSIONS` (file lists only 0.7.0/0.25.0/0.37.0), so "no BREAKING entry" is coherent.
- **Guard constraints:** pi enforcement is real (hooks.json PreToolUse entry + hook-bridge deny), the model-groups entry is manifest-managed `seedOnce:false`, and the override is process-env-only as claimed.
- **Publish/activation:** `bun run check` is the exact canonical↔user-scope comparison (`publish.ts:359-377`); the negative control (skip publish → `OUT OF SYNC` + installed probe red) is valid. `/reload` does hot-reload auto-discovered extensions (pi `docs/extensions.md:7,1397-1416`), so M2's session-staleness detection is sound. B8 proves the project-registry path; the installed-frozen path is separately proven by B7's B3 re-run — the leg separation is correct.
- **Probe falsifiability:** B3 asserts `source === "frozen"` before ids; B4 asserts `source === "project"` + `frameworkVersion === "0.172.0"` + ids, so no leg can pass on the wrong registry; pre-change red states (muse-spark today) are documented. The fresh-scaffold leg (A5→B4(ii)) is corroborative rather than independent of B4(i) (same deliverer path) but is the only probe that exercises a virgin project and closes the no-test-reads-registry gap, as the plan claims.
- **Framework dependency:** the required medium demotion is forced by the shipped validator (exactly-one-preferred-first `model_groups.py:117-144`; active price order + demoted-tail-only with `revisionWatch` `:214-244`; weights-evidence `:248-278`), so PKG-F's rotation shape is coherent with PKG-I's pass condition.