## Verdict

**SHIP-WITH-FIXES** — the registry edit, test changes, and release artifacts are mechanically correct and every narrow oracle passes (111 tests, `validate --all` exit 0, all release gates exit 0, pylint 10.00/10). Two honesty defects remain: a shipped evidence string that this rotation falsifies but the diff did not update, and a false "no member was added or removed" rationale in the new changelog that the same commit's test data contradicts. Neither changes resolution behavior or gate outcomes; both are text-only fixes (the registry fix needs a follow-up release, since registry content is scaffold-affecting).

**MUST — the full-price twin's evidence still calls the contributor "the preferred"**

- `features/common/data/model-groups.json:118` (and the byte-identical mirror `.ai-badger/model-groups.json:118`):
  `"Full-price twin of the contributor preferred; identical weights (weightsId muse-spark-1.3-weights) (source: task-brief 2026-09-05)."`
- Evidence (read): the string was true pre-change (`git show 7718b7d5^:features/common/data/model-groups.json` line 119). After this commit the contributor is demoted in both groups: canonical:179 `"Demoted from medium-tier preferred on 2026-09-21 when deepseek-v4.1-flash took index 0"`, canonical:231 `"now a demoted tail pin in both high and medium"`, and the 0.172.0 changelog says it "stays visible as medium's demoted tail". Nothing in the registry is "the contributor preferred"; the sentence now contradicts the two demotion strings in the same file.
- Failure scenario: the next refresh author (or any consumer reading provenance) looks for a preferred contributor that no longer exists; the registry's evidence record contradicts itself at three points.
- Concrete fix: reword both copies, keeping the `weightsId` string (the validator requires it in evidence): `"Full-price twin of the demoted contributor (demoted tail in medium and high); identical weights (weightsId muse-spark-1.3-weights) (source: task-brief 2026-09-05; medium demotion 2026-09-21)."` Then re-scaffold the mirror/manifest and ship it as its own release — the commit itself classifies registry content as scaffold-affecting.

**SHOULD — "no member was added or removed" is false and is now pinned by FOLD-17**

- `docs/changelog/0.172.0-medium-tier-prefers-deepseek-v41-flash.md:9-10` (repeated in the commit message): `"registryVersion stays 1 — no member was added or removed and no price moved; the preferred index rotated."`
- Evidence (run): pre/post per `git show` — medium went **9 → 10** group entries (`deepseek-v4.1-flash` added, contributor re-appended), registry total **18 → 19**; the distinct registry-wide id set is stable only because deepseek already ships in high. The same paragraph says the contributor "stays visible as medium's demoted tail", i.e. an entry was appended — self-contradiction. In this codebase "member" is a group entry (`model_groups.py` `_validate_member`, errors name `groups.medium[9]`). The copied 0.168.0 sentence was false too: that release added a genuinely new id (high 3 → 4; distinct set changed), so this is a recurring inaccurate template. FOLD-17 now freezes the conclusion (`tests/test_model_groups_registry.py:358` `assert doc["registryVersion"] == 1`), while `docs/changelog/0.165.0-model-groups-registry.md:31` says "a human re-orders and bumps `registryVersion`".
- Failure scenario: a maintainer follows the rationale + pin and concludes entry-set changes never bump `registryVersion`, while the only doc stating the bump rule says the opposite; the pin rests on a premise the data disproves.
- Concrete fix: state the actual invariant ("no new model id entered the registry — deepseek-v4.1-flash already ships in high — and no price moved; the preferred index rotated and the displaced contributor was appended as the demoted tail") in the next release note/erratum, and document the `registryVersion` rule once in a current surface (schema description or the task skill). If the intended rule is entry-set versioning, the honest resolution is `registryVersion: 2` plus a deliberate pin update — the current combination is the one thing that can't stand.

**INFO — L0-5's name now describes a fixture, not the shipped seed**

- `tests/test_model_groups_registry.py:169-170` (`# L0-5 sonnet-5 last in medium` / `test_sonnet_5_is_last_in_medium`).
- Evidence (read/run): the test loads a tmp fixture where sonnet is last and passes — not vacuous, not false within its fixture. But the shipped medium tail is now `muse-spark-1.3-contributor`, and L0-11 asserts sonnet at `[-2]` (`tests/test_model_groups_registry.py:332-338`), so the suite reads as self-contradictory. The commit left it deliberately per FOLD-1.
- Concrete fix: rename to an invariant-shaped name (e.g. `test_demoted_tail_is_last_in_medium`) or annotate `(fixture)`.

## Checked-clean

- **Validator invariants (question 1)** — `python3 tooling/validate.py --all` → exit 0, including `ok features/common/data/model-groups.json` and `ok model-groups registry invariants`. Independently probed in memory (no files edited), each rule fires: prefix-less id → `must match ^openrouter/<vendor>/<name>$`; removed `revisionWatch` → `a demoted tail member must carry revisionWatch: true`; stripped `weightsId` from evidence → `shares weightsId "muse-spark-1.3-weights" but its evidence never names it`; swapped active prices → `price order violated`; preferred moved off index 0 → `must list its preferred member first`; demoted entry moved mid-list → `demoted members are tail-only`. Baseline validates to `[]`.
- **Active order / tails** — medium active keys `(0.15,0.6) ≤ (0.435,0.87) ≤ (0.57948,1.73844) ≤ (0.9918,1.9836) ≤ (1.25,4.25) ≤ (1.4,4.4) ≤ (2,10) ≤ (2,12)`; demoted `sonnet[-2]` and `contributor[-1]`, both `revisionWatch: true`. `frameworkVersion` 0.172.0 == `VERSION` 0.172.0.
- **FOLD-5 (question 2)** — observed `FOLD-5 id eq: True | pricing eq: True | pricing: {'inputPerM': 0.15, 'outputPerM': 0.6, 'currency': 'USD'}`. The assertion holds on the full pricing dicts, not just id.
- **Advisory table (question 2)** — parsed exactly 3 rows; every short resolves to exactly one member and matches `deciding[0]`/`deciding[-1]`: `low glm-5.3-flash→z-ai/glm-5.3-flash`, `low haiku-4.5→claude-haiku-4.5`, `medium deepseek-v4.1-flash→deepseek-v4.1-flash`, `medium terra→gpt-5.6-terra`, `high deepseek-v4.1-flash`, `high fable-5.1→claude-fable-5.1` (unique=True, match=True each). Bands recompute: `$0.027 → $0.44` and `$0.027 → $2.00` correct.
- **Test edits** — L0-4 rename/fixture is valid and exercises the demoted-tail price exemption (cheap demoted tail behind a pricier active pin); L0-11 was strengthened, not weakened (exact ids/prices, two tail pins, FOLD-5, `registryVersion` pin); T-DUP docstring is accurate about the shipped reuse (fixture still uses preferred mode, which is legal); the integration `CHANGELOG` repoint keeps low/high coverage and moves the medium row to the rotated pin; the 7b docstring now honestly states the matrix follows shipped pins (it never was a rotation pin). No assertions were removed. Suite matches the commit's "111 passed" claim exactly.
- **Release artifacts (question 3)** — `VERSION`, `index.json`, both `.claude-plugin/*`, `.ai-badger/config.json`, and all scaffolded stamps are 0.172.0; `version_sync --check` → "version literals up to date"; `changelog_index --check` → "281 versions, 329 entries"; `index_build --check` → up to date; `docs_guard.py` → "485 document(s) scanned … PASS"; `scaffold_freshness_guard.py` → "2612 path(s) compared … PASS"; `release_guard.py` → exit 0 (tag `ai-badger--v0.172.0` already exists).
- **Mirror + manifest** — `diff features/common/data/model-groups.json .ai-badger/model-groups.json` empty (groups equal, stamp 0.172.0). Manifest structural parity confirmed: no keys added/removed; only 278 `frameworkVersion` stamps, `frameworkCommit`, `configHash`, and the one `model-groups.json` entry hash changed.
- **Merge fidelity** — `git diff --quiet 7718b7d5 6502b0ea` → exit 0: the reviewed tree is identical to the merged squash (#492) and to the commit targeted by annotated tag `ai-badger--v0.172.0`.
- **Oracles run** — `python3 -m pytest tests/test_model_groups_registry.py tests/test_model_tiers_integration.py -q` → `111 passed in 3.90s`; `pylint` on both files → `10.00/10`.
- **Not independently verifiable** — the commit message's RED-before-green ordering (no reflog evidence in the worktree); informational only. Top-level `measuredAt: 2026-09-21` with prices dated 2026-09-11 is defensible under the schema's "pricing/evidence date" wording and the per-entry `measuredAt` values — no finding.

Read-only review: no files were edited, nothing pushed or merged.