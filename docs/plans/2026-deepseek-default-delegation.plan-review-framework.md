## Verdict

READY-WITH-FOLDS — the registry edit itself is validator-correct (independently re-verified in memory), but two planned items are provably wrong and must be folded before execution.

## Findings

**MUST — L0-5's planned assertion is false against its own fixture (and against the current one).**
Evidence: `plan-framework.md` Test impact, L0-5 rows; current `tests/test_model_groups_registry.py:166-175` has `sonnet-5` carrying `status="demoted"`, so it can never appear in `deciding`; the planned fixture (deepseek preferred, sol, sonnet-demoted, contributor-demoted appended) leaves `deciding = [deepseek-v4.1-flash, gpt-5.6-sol]`. In-memory probe on the plan's literal fixture: `validator errors on planned L0-5 fixture: NONE` / `deciding ids: ['deepseek-v4.1-flash', 'gpt-5.6-sol']` / `planned assertion deciding[-1].endswith('claude-sonnet-5') -> False`. The same replacement assertion also fails on the *current* fixture (`deciding[-1]` is `sol` there too). `plan-verification.md` A2 contradicts the framework annex: "`:166` L0-5 is fixture-only and needs no change".
Fix: either drop the L0-5 edit entirely (its 3-element fixture still tests the demoted-tail exemption and passes), or, if mirroring the seed, make the assertion match the fixture: last *deciding* is `gpt-5.6-sol`, plus a demoted-tail assertion (`demoted[-1]` contributor, `medium[-2]` sonnet) if the contributor is appended. Do not ship the quoted replacement.

**MUST — the "green focused run" (step 3) cannot be green before the self-scaffold; the plan's own F7 says so.**
Evidence: `plan-framework.md` Release sequence steps 3 and 5; `tests/test_model_tiers_integration.py:204-210` compares shipped `.ai-badger/model-groups.json` `frameworkVersion` to `VERSION` and `:212-219` compares shipped groups to the canonical seed. After step 2, `VERSION` is `0.172.0` and the seed is rotated, but the mirror is still `0.171.0`/old groups (it is only regenerated at step 5, and `plan-framework.md` itself forbids hand-editing it; generated-file guard at `.ai-badger/skills/welcome-ai-badger/scripts/generated_file_guard.py`). `plan-verification.md` F7: "`:204` red resp. `:212` red" for exactly these two mutations. The 0.168.0 precedent (commit `c835effc`) recorded RED → ritual → green, never a green focused run before the scaffold.
Fix: swap steps 3 and 5 (changelog_index → version_sync → self-scaffold → focused green run), or state step 3 is expected red on these two tests and record green after step 5.

**SHOULD — the seed oracle is expectation-only; a coordinated wrong pin passes every framework gate.**
Evidence: L0-4/L0-5/T-DUP are fixture tests (`tests/test_model_groups_registry.py:141-176`, `:386-396`) that never read the seed; the only seed oracle is L0-11's hardcoded id/price lists (`:292-338`), edited in the same change; the integration advisory-join (`tests/test_model_tiers_integration.py:236-259`) compares the changelog table with the same shipped registry, also edited in the change; the validator constrains structure only. `plan-verification.md` F10 already concedes "coordinated double-edit … stays green … change-detectors, not independent oracles", and F4/F5 only revert the seed, proving non-vacuity, not independence.
Fix (cheap): encode the task's actual intent relation in L0-11 — `assert groups["medium"][0]["id"] == groups["high"][0]["id"]` and equal `pricing` — which kills the "medium and high rotated to different models" class of coordinated edit; the exact vendor id still relies on review/F9. While there, keep sonnet's `status`/`revisionWatch` assertions at `medium[-2]` (the planned tail edit keeps only its id).

**SHOULD — PKG-F AC #6 has no "CI green" clause and its named gate list omits lanes the auto-tag cannot retroactively block.**
Evidence: `plan.md` PKG-F AC #6 lists validate/version_sync/changelog_index/index_build/release_guard/scaffold_freshness_guard/docs_guard + pytest + pylint, never CI. `.lefthook/pre-push/verify.sh:65` `LOCAL_LANES` adds `plugin-skills deps paths workflows tdd js pi-ts` and the two `.mjs` agent-instruction validators inside `lane_validate` (`verify.sh:258-266`); `pylint pytest journey` are CI-only (`verify.sh:80`) and `RELEASING.md:29-33` tags automatically from `main` after the merge.
Fix: add "CI green on the PR (all workflows)" to AC #6 and either require the pre-push hook / `verify.sh all` or enumerate the omitted lanes. Low cost; closes the gap between "local gates green" and "tagged".

**INFO — `measuredAt` semantics drift from the 0.168.0 precedent.**
Evidence: plan sets top-level `measuredAt` 2026-09-21 and the new preferred's per-entry `measuredAt` 2026-09-21 while reusing the 2026-09-11 price; `schemas/model-groups.schema.json:16-21` calls the top-level value the "pricing/evidence date for the whole file", and 0.168.0 stamped the new high entry 2026-09-11 (the price date). No validator/test reads it (L0-11 ignores `measuredAt`).
Fix: use 2026-09-11 for the new preferred (mirrors 0.168.0 and the evidence), or say in the changelog that 2026-09-21 is the rotation date, not a re-measurement.

**INFO — managed-tree inventory misses `.ai-badger/config.json`; anchor drift in the L0-11 table.**
Evidence: `git show c835effc --stat` includes `.ai-badger/config.json` (self-scaffold stamps it via `scaffold.py:741`), but `plan-framework.md`'s "Other files" table does not name it. The L0-11 medium price list is at lines 315-316, not "315-318".
Fix: add config.json to the expected churn list; correct the anchor.

**INFO — inter-annex inconsistencies worth reconciling in the lane report.**
Evidence: `plan-framework.md` uses system `python3` (verified: no `.venv` in the worktree; system python3 has pylint 4.0.8, pytest 9.1.1, jsonschema), while `plan-verification.md` A1/A4 use the main checkout's `.venv/bin/python3` (`verify.sh:_resolve_python` falls back there). A4 also omits `docs_guard.py` from its gate chain and reads as self-scaffold-then-version_sync. All variants work; pick one and paste that in the evidence.
Fix: standardize the interpreter and the gate list across annexes; the framework annex's order (version_sync before self-scaffold) is the correct one.

**INFO — budget realism: one lane is sound, conditional on the two MUST fixes.**
Evidence: the content+ritual shape matches the single-lane `c835effc` precedent (20 files, self-scaffold + PR + auto-tag), but this plan adds the full ~5.5k-test suite + pylint in-lane (`plan-verification.md` notes 0.168.0 explicitly left those to the integration step), a 12-lane pre-push hook with a 1200s deadline (`verify.sh:61-80`), and a review round; `.ai-badger/state.json:3` carries two known local flakes. Feasible at high effort once the step-3 red is removed and R9's "CI is the arbiter" rule is applied verbatim.
Fix: keep one lane; budget the pre-push hook explicitly and state that flake attribution compares against baseline or CI.

## Checked-clean

- **Registry edit is validator-correct.** Re-ran `validate_registry` + `bl.validate(doc, schemas/model-groups.schema.json)` in memory on the plan's literal proposed document: `VALIDATOR ERRORS: NONE -> PASS`, `SCHEMA ERRORS: NONE -> PASS`; medium order `deepseek, mimo-pro, ds-pro-0813, ds-pro, muse-spark-1.3, glm, sol, terra, sonnet, contributor`; `preferred medium == preferred high == deepseek`. (Preferred-first, non-decreasing active `(inputPerM, outputPerM)`, demoted-tail-only + `revisionWatch`, weights-identity evidence all hold.)
- **Weights identity:** after the edit the three `muse-spark-1.3-weights` holders are medium[4], medium[9], high[3]; the planned medium-tail and corrected high-tail evidence both name the string; validator PASS.
- **Demoted tail placement:** append-at-end leaves sonnet-5 at `[-2]` and contributor at `[-1]`, both demoted + `revisionWatch: true`; the alternative insert-before-sonnet-5 also validates (both are tail-only), and append mirrors `git show c835effc`'s "displaced preferred goes last".
- **HIGH-tail evidence fix is necessary and correctly excluded from tests:** `features/common/data/model-groups.json:218` says "remains medium's preferred" (false after the rotation); the planned replacement keeps the `weightsId` mention; no test pins the string.
- **Advisory table:** planned medium `deepseek-v4.1-flash → terra`; after rotation the dearest *deciding* medium pin is `gpt-5.6-terra`; dash-token uniqueness holds for all three rows (`deepseek`/`v4.1`/`flash` matches only `deepseek-v4.1-flash` in medium; `terra`/`fable-5.1`/`haiku-4.5` unique too); the third column is unparsed.
- **CHANGELOG repoint is required and sufficient:** `tests/test_model_tiers_integration.py:43` selects exactly one file; 0.165.0/0.168.0 entries stay historical (no gate parses them once line 43 moves).
- **Release-ritual order (apart from the misplaced green run):** changelog_index → version_sync → self-scaffold strictly last → gates; `version_sync.py:88-110` catches a skipped scaffold via manifest/stamp mismatches; `scaffold.py` derives stamps from `index.json` (`:282,:362,:741`), so version_sync-first is load-bearing (0.167.1 precedent). `release_guard` fetch-tags precondition and post-merge tag verification are present; `VERSION` 0.172.0 is forward of tag `ai-badger--v0.171.0`.
- **No missing framework file:** `git show c835effc --stat` is the precedent inventory (managed tree + config/manifest/model-groups, `.claude-plugin/*`, VERSION, changelog entry + README, seed, index.json, both test files); the plan covers all of it (config.json only implicitly). `state.json` was absent from the 0.168.0 release commit, so deferring it to task-close matches precedent.
- **Other model-groups readers are content-agnostic:** `tests/test_model_tiers_docs.py`, `tests/test_pi_agents.py`, `tests/test_scaffold_manifest.py`, `gates/consumer_journey.py` reference the path/manifest, not pins; `grep -rn "muse-spark"` finds the id only in the seed, its mirror, the two managed test files, and historical docs/work/changelogs.
- **No other version-lineage literals:** `grep -rn "0.171.0"` outside `docs/`, `.ai-badger/`, `.claude-plugin/` hits only the seed and `index.json`; no shipped `features/**` version pin; no `package.json` at framework root; no test hardcodes 0.171.0.
- **No `BREAKING_VERSIONS` entry needed** (minor class, re-scaffold recommended only), matching 0.168.0; `BREAKING_VERSIONS` currently ends at 0.37.0.
- **Environment supports the lane:** no `.venv` in the worktree; system `python3` has pytest 9.1.1/pylint 4.0.8/jsonschema; `bun`, `node`, `npm` present for the pre-push lanes.